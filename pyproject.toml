[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "simulated-world-evolve"
version = "0.1.0"
description = "Simulates characters lives turn-by-turn on a deterministic engine; an LLM acts where no script can."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0"
]
