# Showrunning — reference index

Deep files to author next, to the `docs/agent-toolboxes.md` standard. Each entry: core idea · full structure · worked example · combines-with · cautions · sources.

- **the-production-loop.md** — the end-to-end loop (concept → world → cast → plan → simulate-to-beat → cut → render → assemble) and the inner scene loop; phase ordering & dependencies; which specialist is called at each step and with what scoped input.
- **gates-and-checkpoints.md** — gate design: pass conditions & fail actions for setup/beat/canon/coherence/cut/render/finish; quality-gate theory; the acceptance criteria as the finish gate; mapping a gate to a real `settings.json` enforcement hook.
- **the-notes-system.md** — the notes schemas (canon ledger, production journal, story-map, threads, continuity register); notes-as-save-file; event sourcing; the world-truth vs process-truth split; resumability.
- **thread-and-promise-tracking.md** — tracking open threads, promises/setups, foreshadow→payoff, and mystery reveal schedules; the register of what you owe the reader; closing the loop on every promise.
- **adaptive-replanning.md** — destination-fixed / route-discovered; when to revise a beat (a faithful refusal) vs re-place circumstance; holding the arc while the route changes; logging revisions.
- **orchestration-patterns.md** — orchestrator-owns-invariants; scoped subagent handoff & context-scoping; the walls (beat-blind simulator, POV-bound narrator); admitting only gated candidates; resumability from the notes.
