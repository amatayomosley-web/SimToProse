# Continuity Register — &lt;book-slug&gt;

> A **working index (process-truth)** of facts fixed as the bible grows from the sim — lazy-resolved details, once decided (`design.md` granularity rule; `docs/world-state-ledger.md`). The authoritative home of each fixed fact is the world bible, or the DB event that fixed it — never this file. The register exists so the continuity-critic and the showrunner can check quickly, without re-deriving.

<!-- fact — decided <when / why> — first appears <scene> -->
